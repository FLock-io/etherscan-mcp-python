# Core dependencies
mcp>=1.0.0
httpx>=0.24.0
pydantic>=2.0.0
python-dotenv>=1.0.0

# HTTP server dependencies (for HTTP transport)
uvicorn[standard]>=0.27.0

# Optional development dependencies
# Install with: pip install -r requirements.txt -r requirements-dev.txt